# Excel Tools 통합 웹앱

PlayAuto 업무 자동화 도구 모음 - 웹 버전

## 🎯 기능

### 1️⃣ 스타배송 필터

- "판매자 스타배송"으로 시작하는 행 자동 삭제
- 드래그 앤 드롭 지원
- 실시간 통계 표시

### 2️⃣ 송장 분류

- 주문 엑셀을 담당자별로 자동 분류
- 설정 파일 기반 상품 매칭
- 합배송, 복수주문 자동 감지
- 분류 실패 건 별도 관리

### 3️⃣ 기능 3

- 준비 중...

---

## 🚀 Render 배포

### 1. GitHub 레포지토리 생성

```bash
git init
git add .
git commit -m "Initial commit: Excel Tools 웹앱"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/excel-tools.git
git push -u origin main
```

### 2. Render 배포

1. [render.com](https://render.com) 가입 (GitHub 연동)
2. Dashboard → **New +** → **Web Service**
3. GitHub 레포 연결
4. 설정:
   - **Name**: `excel-tools`
   - **Runtime**: Python
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app:app`
5. **Create Web Service** 클릭

배포 완료 후 URL: `https://excel-tools.onrender.com`

---

## 📦 파일 구조

```
excel-tools/
├── app.py                          # Flask 서버
├── requirements.txt                # 의존성
├── render.yaml                     # Render 설정
├── playauto_settings_v4.json      # 송장 분류 설정
├── README.md                       # 문서
└── templates/
    └── index.html                  # 통합 UI
```

---

## 🔧 로컬 실행

```bash
# 의존성 설치
pip install -r requirements.txt

# 서버 실행
python app.py

# 브라우저에서 열기
# http://localhost:5000
```

---

## 📋 송장 분류 사용법

### 1단계: 설정 파일 업로드

- `playauto_settings_v4.json` 파일 업로드
- 담당자 및 상품 규칙 정의

### 2단계: 주문 파일 업로드

- 엑셀 파일 업로드 (필수 컬럼: 주문번호, 상품명, 수량)
- 자동 분류 실행
- 담당자별 시트로 구분된 엑셀 다운로드

### 설정 파일 예시

```json
{
  "work_order": [
    "송과장님",
    "영재씨",
    "강민씨",
    "부모님",
    "합배송",
    "복수주문",
    "분류실패"
  ],
  "work_config": {
    "송과장님": {
      "products": [
        {
          "brand": "꽃샘",
          "product_name": "밤 티라미수",
          "order_option": "All"
        }
      ]
    }
  }
}
```

---

## ⏰ UptimeRobot 설정 (24시간 유지)

1. [uptimerobot.com](https://uptimerobot.com) 가입
2. **Add New Monitor**
3. 설정:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: Excel Tools
   - **URL**: `https://YOUR-APP.onrender.com/health`
   - **Monitoring Interval**: 5 minutes
4. **Create Monitor**

---

## 💰 비용

| 서비스           | 요금   |
| ---------------- | ------ |
| Render 무료 티어 | $0     |
| UptimeRobot 무료 | $0     |
| **총 비용**      | **$0** |

---

## 🎨 UI 특징

- 다크 테마 디자인
- 드래그 앤 드롭 지원
- 실시간 진행률 표시
- 모바일 반응형
- 네온 컬러 강조

---

## 🛠 기술 스택

- **Backend**: Flask 3.0
- **Frontend**: Vanilla JS + CSS3
- **Data**: Pandas, OpenPyXL, NumPy
- **Deploy**: Render + Gunicorn

---

## 📝 업데이트 로그

### v1.0 (2024-12-16)

- 스타배송 필터 기능
- 송장 분류 시스템 통합
- 웹 UI 구현
- Render 배포 설정
